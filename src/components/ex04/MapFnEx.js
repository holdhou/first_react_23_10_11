const nums = [1,2,3,4,5];


export const MapFnEx=()=>{
    /* *map()
    배열을 기준으로 배열의 요소겂울 새로운 값으로 반환하는역할을함 */
    nums.map((n) => console.log(n+"1"));
    return(
        <div>

        </div>
    )
}
